---
sha: 8da403223ce74bd878836b10e87ea641c8ff8de4
short_sha: 8da4032
audited_at: 2026-05-18
auditor_model: claude-opus-4-7
verdict: findings-applied
codex_status: dual-reviewer
audited_by: audit-review v1
---

# Audit: feat: use CcxtOcx exchange-scoping macro (Task 6b) (#10)

**Original commit:** 8da4032 — `feat: use CcxtOcx exchange-scoping macro (Task 6b) (#10)`
**Author:** E.FU
**Source PR:** [#10](https://github.com/ZenHive/ccxt_ocx/pull/10)
**Linked issue:** none parsed
**Files touched:** several (lib/ccxt_ocx.ex, lib/ccxt_ocx/macros/use.ex + macros/exchange_caps.ex new, lib/ccxt_ocx/declarations/compile.ex updated, test/, .sobelow-skips, CHANGELOG.md, ROADMAP.md, roadmap/{data.json,tasks.toml}, roadmap/plans/6b-use-ccxtocx-entrypoint.md)
**LOC:** +1027

## Findings

| # | Pri | Category | File:Line | Description | Resolution |
|---|-----|----------|-----------|-------------|------------|
| 1 | 8 | Bug (security) | lib/ccxt_ocx/declarations/compile.ex:351 | `exchange_id_from_path/1` was `@doc false` + `def` — still publicly reachable + uses `String.to_atom/1` on its input (DoS vector — unbounded atoms). All callers are in the same module. Codex-corroborated. | Applied: `def` → `defp`. The function is now unreachable from outside the module; the DoS surface is closed structurally. |
| 2 | — | discuss-design (filed as rmap follow-up) | lib/ccxt_ocx.ex:39 + generated modules | Codex flagged public `__using__/1` macro and generated `__exchange_id__/0` / `__generated_by_task_6b__/0` as missing `@doc`/`@spec`. `mix doctor` is green on the project, which suggests Doctor accepts what's present; auditing each gap requires a separate pass. Project rule: never add `@doc false` to mask. | Filed as rmap follow-up for a focused doc-completeness audit. |
| 3 | — | discuss-trivial (dropped, not corroborated) | .sobelow-skips | Codex claimed stale duplicate entries (`.sobelow-skips:13`). Audit ran `mix sobelow --mark-skip-all` — file regenerated byte-identical; `sort | uniq -c` shows zero duplicates. Codex over-flag. | Dropped (verified). |
| 4 | — | discuss-trivial (filed as rmap follow-up) | lib/ccxt_ocx/macros/exchange_caps.ex:192 | Codex flagged hardcoded `timeout: 5000` in the JS probe as a duplicate of the literal in `BundleSurface.Compile`. Cosmetic extraction — would benefit from a shared module attribute but not urgent. | Filed as rmap follow-up. |

Pre-merge bot-finding triage (PR #10):

| Bot finding | Status |
|---|---|
| Copilot: `caps_path/1` builds from atom — path traversal + arbitrary code exec primitive | ✅ Addressed at shipped SHA — `fetch_or_build/1` calls `validate_known_id!(id)` (rejects non-`known_exchange_ids/0` atoms) BEFORE `caps_path/1` and `Code.eval_file/1`. |
| Copilot: no synchronization in `fetch_or_build`/`build_and_cache!` — partial-read race | ⚠️ Partial — temp-sibling + rename in `build_and_cache!/2` prevents partial-read evaluation; no per-id lock so concurrent builds are still possible (idempotent though). Codex confirmed acceptable. |
| Copilot + CodeRabbit: `.sobelow-skips` wrong line numbers | ⚠️ Codex confirmed missing skips were added later via subsequent regens; current `mix sobelow --exit Low` is clean. |
| Codex GH bot P2: integration probe test runs on default `mix test` | ✅ Addressed — moved to `ExchangeCapsIntegrationTest` with `@moduletag :integration`; default test_helper excludes `:integration`. |
| Copilot: `use_test.exs async: true` with QuickBEAM side effects | ✅ Addressed — file now contains only pure validation; the QuickBEAM probe lives in its own `async: false` integration module. |
| Copilot: `exchange_id_from_path/1` public + `String.to_atom/1` DoS vector | ❌ Was NOT addressed pre-merge — fixed by this audit (finding 1 above). |

## Auto-applied fixes

- lib/ccxt_ocx/declarations/compile.ex: `def exchange_id_from_path/1` → `defp` (dropped `@doc false`, kept the explanatory comment about the DoS-vector rationale).

## Discuss-tier resolutions

- (filed as rmap follow-ups: `__using__/1` + generated-module doc-completeness audit; probe-timeout extraction.)

## Codex second-opinion

Status: dual-reviewer
Task: `task-mpb28d4i-uvvt4b` (7m 22s)
Corroborated findings: — (none with Claude; finding 1 was solo-Codex, verified by direct code reading)
Codex-only findings (verified, applied): 1
Codex-only findings (filed as rmap follow-up): 2, 4
Codex-only findings (discarded as over-flag): 3
